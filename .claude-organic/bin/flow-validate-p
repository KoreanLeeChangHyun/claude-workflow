#!/usr/bin/env bash
ROOT="$(git -C "$(dirname "$0")/.." rev-parse --show-toplevel 2>/dev/null)" \
  || ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
exec python3 "$ROOT/.claude.workflow/scripts/flow/prompt_validator.py" "$@"
