#!/bin/bash
# AskUserQuestion Slack 알림 (thin wrapper)
# 실제 로직: .claude/scripts/slack/slack-ask.sh
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"
cat | bash "$PROJECT_ROOT/.claude/scripts/slack/slack-ask.sh"
