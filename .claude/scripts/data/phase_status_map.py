"""
phase_status_map.py - 히스토리 동기화용 Phase-상태 매핑 및 테이블 헤더 정의

history_sync에서 히스토리 테이블을 생성할 때 사용하는 테이블 헤더와
Phase를 한글 상태 텍스트로 변환하는 매핑을 정의합니다.
이 모듈은 순수 상수만 정의하며, 다른 모듈을 import하지 않는 leaf 모듈입니다.

카테고리:
    테이블 헤더 라인   - HEADER_LINE
    테이블 구분선      - SEPARATOR_LINE
    Phase-상태 매핑    - PHASE_STATUS_MAP
"""

# =============================================================================
# 히스토리 테이블 헤더 라인
# =============================================================================
HEADER_LINE = "| 날짜 | 작업ID | 제목 & 내용 | 명령어 | 상태 | 계획서 | 질의 | 이미지 | 보고서 |"

# =============================================================================
# 히스토리 테이블 구분선
# =============================================================================
SEPARATOR_LINE = "|------|--------|------------|--------|------|--------|------|--------|--------|"

# =============================================================================
# Phase를 한글 상태 텍스트로 변환하는 매핑
# =============================================================================
PHASE_STATUS_MAP = {
    "COMPLETED": "완료",      # 작업 완료
    "REPORT": "진행중",       # 보고서 작성 단계
    "STALE": "중단",          # 만료/중단됨
    "WORK": "진행중",         # 작업 실행 단계
    "PLAN": "진행중",         # 계획 수립 단계
    "INIT": "진행중",         # 초기화 단계
    "CANCELLED": "중단",      # 사용자에 의한 취소
    "FAILED": "실패",         # 실패
    "UNKNOWN": "불명",        # 알 수 없는 상태
    "NONE": "불명",           # 상태 없음
}
